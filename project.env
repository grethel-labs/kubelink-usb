# Central project metadata — single source of truth for version, registry,
# and image names.  Consumed by Makefile, CI workflows, and sync-chart-version.
VERSION=0.1.0
REGISTRY=ghcr.io/grethel-labs
CONTROLLER_IMG=kubelink-usb-controller
AGENT_IMG=kubelink-usb-agent
